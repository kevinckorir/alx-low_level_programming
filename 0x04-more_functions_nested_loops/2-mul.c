#include "main.h"

/**
 * mul - multiplies two integers
 * @a: takes one integer as parameter
 * @b: second integer param
 * Return: multiple of the two parameters.
 */
int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
